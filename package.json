{
  "name": "student-management-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "npm --prefix backend run build",
    "prestart": "npm --prefix backend run build",
    "start": "npm --prefix backend start"
  }
}
